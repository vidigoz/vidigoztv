# Local Netlify folder
.netlify

# OS files
.DS_Store

# Dependencies
node_modules/
# Netlify functions necesitan sus deps vendidas (sin build step que las instale)
!netlify/functions/node_modules/

# Secrets / local notes with API keys — nunca subir
*.env
.env.*
keys.txt
*keys*.txt
terminalruns.txt
*secret*
*credential*
*.pem
vidiclip/.claude/
